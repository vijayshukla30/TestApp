export type ThemeMode = "light" | "dark";

export type Theme = {
  mode: ThemeMode;
  background: string;
  surface: string;
  text: string;
  subText: string;
  border: string;
  primary: string;
  shadow: string;
};
