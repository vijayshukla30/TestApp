import { colors } from "./colors";
import { Theme } from "./types";

const light: Theme = {
  mode: "light",
  background: "#F9FAFB",
  surface: "#FFFFFF",
  text: "#111827",
  subText: "#6B7280",
  border: "#E5E7EB",
  primary: "#6366F1",
};

export default light;
